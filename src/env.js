const BASE_URL = 'http://localhost:8080';
const API_URL = `${BASE_URL}/api`; // this means that the API_URL is http://localhost:8080/api and that the endpoint for the characters is http://localhost:8080/api/characters
export { BASE_URL, API_URL };